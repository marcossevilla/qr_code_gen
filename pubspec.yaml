name: qr_code_gen
description: Flutter package which manages qr_code_gen domain and data.
version: 1.0.0

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^1.2.0
  meta: ^1.1.8
  qr_flutter: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^4.1.3
  very_good_analysis: ^1.0.3
